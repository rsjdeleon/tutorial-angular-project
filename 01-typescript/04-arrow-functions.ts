// common approach
let log = function(message) {
    console.log(message);
}

// arrow function approach 1
let doLog1 = (message) => console.log(message);

// arrow function approach 2
let doLog2 = (message) => {
    console.log(message);
}